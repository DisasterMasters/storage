# This is a comment, comments are any line starting with a hash character

# The first non-empty line is the collection to manipulate
Statuses_SomeHurricane_C

# All subsequent lines are of the form "from -> to", where "from" and "to" are
# The key names you are moving values from and to, respectively
foo -> bar
bar -> baz

# The parser is pretty robust about whiespace, as long as the key names don't
# have any space leading/trailing the string
   big_space        ->             no_match
no_space->fine_too

